var assert = require("chai").assert;

describe('recommendation', function(){
	it("Displays recommendations Image.", async function create(){
		var showImages = true
		if (showImages){
			var Recommendations = true;
			if (Recommendations){
				var testResult = true;
				}
			else{
				var testResult = false;
			}
		}
	 assert.equal(testResult,true)
	})
	
 });