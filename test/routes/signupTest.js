var signup = require("../routes/signup");
var expect = require("chai").expect;

describe('signup', async function(){
  it("If inputs are valid it should redirect to login page with success message.")
});
